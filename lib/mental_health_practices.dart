import 'package:flutter/material.dart';

class MentalHealthPractices extends StatelessWidget {
  const MentalHealthPractices({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Good Mental Health Practices')),
      body: const Center(child: Text('List of Good Mental Health Practices')),
    );
  }
}
